#pragma once

#include "commands/commands.hpp"

/*
 *
 *
 * This is the same thing as commands/commands.hpp
 * just for the package subdirectory so instead of
 * it just being namespace commands its 
 * namespace commands::package\
 * 
 * 
 */