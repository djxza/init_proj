#pragma once

#include "utils.hpp"

/*  
 *
 *
 * this is nase file for commands folder.
 * there is a namespace commands. and for
 * subdir in commands there is a corresponding
 * namespace in commands ie. commands::init
 * and often for every file in that subdir
 * there will be a function in commands::subdir::file()
 * or like commands::init::exe().
 * EVERY file in the directory will include
 * this file as some sort of base file.
 * 
 * 
 */